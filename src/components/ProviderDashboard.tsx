/**
 * The clinician-facing view is the attending portal. This alias is kept so
 * existing imports (App.tsx) and the CLINICIAN role naming stay stable.
 */
export { AttendingPortal as ProviderDashboard } from "./attending/AttendingPortal";
